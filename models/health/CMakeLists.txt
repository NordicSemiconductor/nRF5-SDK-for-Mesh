set(HEALTH_CLIENT_SOURCE_FILES
    "${CMAKE_CURRENT_SOURCE_DIR}/src/health_client.c" CACHE INTERNAL "")
set(HEALTH_CLIENT_INCLUDE_DIRS
    "${CMAKE_CURRENT_SOURCE_DIR}/include" CACHE INTERNAL "")

set(HEALTH_SERVER_SOURCE_FILES
    "${CMAKE_CURRENT_SOURCE_DIR}/src/health_server.c" CACHE INTERNAL "")
set(HEALTH_SERVER_INCLUDE_DIRS
    "${CMAKE_CURRENT_SOURCE_DIR}/include" CACHE INTERNAL "")

get_property(core_include_dirs TARGET mesh_core_${PLATFORM} PROPERTY INCLUDE_DIRECTORIES)
set(target_include_dirs
    ${HEALTH_CLIENT_INCLUDE_DIRS}
    ${HEALTH_SERVER_INCLUDE_DIRS}
    ${ACCESS_INCLUDE_DIRS}
    ${CMAKE_SOURCE_DIR}/mesh/app)

add_pc_lint(health_${PLATFORM}
    "${HEALTH_CLIENT_SOURCE_FILES};${HEALTH_SERVER_SOURCE_FILES}"
    "${target_include_dirs};${core_include_dirs}"
    "${${PLATFORM}_DEFINES};${${SOFTDEVICE}_DEFINES}")
