set(RSSI_MONITOR_CLIENT_SOURCE_FILES
    "${CMAKE_CURRENT_SOURCE_DIR}/src/rssi_client.c" CACHE INTERNAL "")

set(RSSI_MONITOR_CLIENT_INCLUDE_DIRS
    "${CMAKE_CURRENT_SOURCE_DIR}/include" CACHE INTERNAL "")

set(RSSI_MONITOR_SERVER_SOURCE_FILES
    "${CMAKE_CURRENT_SOURCE_DIR}/src/rssi_server.c" CACHE INTERNAL "")

set(RSSI_MONITOR_SERVER_INCLUDE_DIRS
    "${CMAKE_CURRENT_SOURCE_DIR}/include" CACHE INTERNAL "")

set(RSSI_MONITOR_UTIL_SOURCE_FILES
    "${CMAKE_CURRENT_SOURCE_DIR}/src/rssi_util.c" CACHE INTERNAL "")

set(RSSI_MONITOR_UTIL_INCLUDE_DIRS
    "${CMAKE_CURRENT_SOURCE_DIR}/include" CACHE INTERNAL "")

set(target_include_dirs
    ${RSSI_MONITOR_CLIENT_INCLUDE_DIRS}
    ${RSSI_MONITOR_SERVER_INCLUDE_DIRS}
	${RSSI_MONITOR_UTIL_INCLUDE_DIRS}
    ${ACCESS_INCLUDE_DIRS}
    ${MESH_INCLUDE_DIRS}
    ${${SOFTDEVICE}_INCLUDE_DIRS}
    ${${PLATFORM}_INCLUDE_DIRS}
    ${${nRF5_SDK_VERSION}_INCLUDE_DIRS}
    ${CMAKE_SOURCE_DIR}/examples/templates)

add_pc_lint(rssi_monitor_${PLATFORM}
    "${RSSI_MONITOR_CLIENT_SOURCE_FILES};${RSSI_MONITOR_SERVER_SOURCE_FILES};${RSSI_MONITOR_UTIL_SOURCE_FILES}"
    "${target_include_dirs}"
    "${${PLATFORM}_DEFINES};${${SOFTDEVICE}_DEFINES}")
